recipe: text_files
select: ["**/*.{md,txt,html}"]
prompt: |
  Return a JSON object with fields:
  - title: detected title or top heading if any
  - summary: a concise 1–2 sentence summary of the content
  Only output valid JSON, nothing else.
expects_json: true
save:
  jsonl: artefacts/${run_id}/text_outputs.jsonl
rag:
  pipeline: local_docs_rag
  top_k_text: 3
